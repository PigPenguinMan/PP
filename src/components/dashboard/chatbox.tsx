import { IDashboardBoxProps } from "../../types/dashboard";

const DashBoardChatBox = (props:IDashboardBoxProps) => {
    return ( 
        <div className={`${props.class}`}></div>
     );
}
 
export default DashBoardChatBox;